extends layout.pug

block content
    h2 Masukan Kata Mutiara
    form(action="kamut_process" method="POST")
        div.form-group
            input(type="text" placeholder="penulis" name="penulis").form-control
            input(type="text" placeholder="judul" name="judul").form-control
            textarea(placeholder="isi" rows="5" cols="100" name="isi").form-control
        button(type="submit").btn.btn-success submit
